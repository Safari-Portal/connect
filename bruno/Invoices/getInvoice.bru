meta {
  name: Retrieve a single invoice with its payments
  type: http
  seq: 2
}
get {
  url: {{baseUrl}}/acc/:acc_id/invoices/:id
  body: none
  auth: inherit
}
params:path {
  acc_id: {{accId}}
  id: 
}
docs {
  Retrieve a single invoice with its payments

  Scopes: invoices:read
}
