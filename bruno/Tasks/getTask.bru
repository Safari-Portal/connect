meta {
  name: Retrieve a single task
  type: http
  seq: 2
}
get {
  url: {{baseUrl}}/acc/:acc_id/tasks/:id
  body: none
  auth: inherit
}
params:path {
  acc_id: {{accId}}
  id: 
}
docs {
  Retrieve a single task

  Scopes: tasks:read
}
