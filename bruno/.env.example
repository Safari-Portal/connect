# Copy to `.env` (untracked) and fill in. Bruno reads it as process.env,
# which the environment files reference for `accId` and `token`.
# Mint a local token with: bundle exec rake connect:token
CONNECT_ACC_ID=
CONNECT_TOKEN=
