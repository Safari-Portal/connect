meta {
  name: Retrieve a single form (metadata only)
  type: http
  seq: 3
}
get {
  url: {{baseUrl}}/acc/:acc_id/forms/:id
  body: none
  auth: inherit
}
params:path {
  acc_id: {{accId}}
  id: 
}
docs {
  Retrieve a single form (metadata only)

  Scopes: forms:read
}
